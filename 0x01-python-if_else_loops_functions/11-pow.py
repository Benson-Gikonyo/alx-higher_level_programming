#!/usr/bin/python3
def pow(a, b):
    """computes a to the power of b and return the value.
    Args:
        a (number): base
        b (number): exponent
    Return: result
    """
    return a ** b
