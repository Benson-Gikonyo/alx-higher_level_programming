#!/usr/bin/node

const dict = require('./101-data').dict;

const obj = {};
let arr = [];
// for (let[value, key]of Object.entries(dict)){
// for (let num = 0; num < dict.length; num++){
//     if (dict[values[num]] in obj[keys]){
//         continue;
//     }else{
//     obj[keys[num]] = dict[values[num]];
//     }
    
// }
// console.log (obj)
